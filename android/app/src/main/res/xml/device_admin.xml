<?xml version="1.0" encoding="utf-8"?>
<device-admin>
    <uses-policies>
        <!-- Prevents the app from being uninstalled while enrolled as device admin -->
        <force-lock />
        <wipe-data />
    </uses-policies>
</device-admin>
