# PearGuard — Session 4 Hand-off (2026-03-22)

## IMPORTANT: Branch State

`master` is 2 commits behind `main`. PR #4 was merged into `main` but not yet synced to `master`. Start next session with:

```bash
git checkout master && git merge origin/main && git push origin master
```

Then verify `git log --oneline -3` shows `df72a5f` (Merge pull request #4) at HEAD.

---

## Completed This Session

### TODO #4 — Parent pairing confirmation banner
`ParentApp.jsx` listens for `child:connected` at the top level (always mounted). Shows "Successfully paired with {name}!" banner for 3s, then auto-navigates to Dashboard.

### TODO #5 — Child pairing confirmation
`Profile.jsx` listens for `peer:paired`, refreshes parents list, shows "Successfully paired with parent!" banner for 3s. Fixed LAN race condition: `pairDoneRef` prevents `acceptInvite` return from overriding `peer:paired` state.

### Startup event buffering (implicit fix for #4/#5)
`app/index.tsx`: bare events that fire before the WebView loads are now buffered in `_pendingWebViewEvents` and flushed on `onLoad`. Fixes silently dropped `peer:paired` / `child:connected` events during startup reconnect (bare.js auto-rejoins persisted Hyperswarm topics before WebView is ready).

### TODO #21 — Clear stale requests
- `requests:list` (child bare method) auto-expires `req:*` entries older than 7 days during scan
- New `requests:clear` bare method deletes all approved/denied entries
- `ChildRequests.jsx` shows "Clear resolved" button when any resolved entry exists

### TODO #10 — Parent Requests tab
- New `RequestsTab.jsx`: pending time requests first with Approve/Deny (via `app:decide`), resolved below with Approved/Denied label; refreshes on `time:request:received`
- Added to `ChildDetail.jsx` between Apps and Schedule
- `alerts:list` now includes `status` field for `time_request` entries so RequestsTab can show Approved vs Denied
- "Alerts" tab renamed "Activity"; Approve/Deny removed — Activity is now informational only

---

## New TODOs Added

| # | Description |
|---|-------------|
| #28 | Prevent child from clearing app storage (investigate DevicePolicyManager) |
| #29 | Bug: initial pairing sends "app installed" notification for every app in the batch |
| #30 | Design decision: default policy (approve/deny/pending) for apps at initial pairing |
| #31 | Parent setup: require override PIN before first use |
| #32 | Parent-initiated unpair / remote deactivation of child |
| #33 | Bug: selected tab highlight remains after leaving ChildDetail |
| #34 | Bug: tapping time-request notification on parent routes to Activity tab, not Requests tab |
| #35 | Child: tapping "Request Approved" notification should open the approved app |

---

## Test State

- **206 passing, 10 failing** (all pre-existing: invite, pairing, Settings, AddChildFlow, "New Request" button stubs)
- Both devices have the latest build installed

## ADB Device Serials

| Device | Serial | Role |
|--------|--------|------|
| Pixel | `53071FDAP00038` | Parent |
| TCL | `4H65K7MFZXSCSWPR` | Child |

---

## Suggested Next TODOs

1. **#34** — Notification routing: change tap target from `alerts` → `requests` in `index.tsx` (quick)
2. **#33** — Tab highlight bug in ChildDetail (investigate `initialTab` + unmount/remount)
3. **#31** — Force PIN at parent setup (`app/setup.tsx` wizard step)
4. **#35** — Child "Request Approved" notification → open approved app
