# PearGuard — Session 6 Hand-off (2026-03-23)

## Branch State

`master` is synced with `main` at commit `76e27ae` (Merge pull request #6).

---

## Completed This Session

### TODO #36 — Pairing banner only shows on first pairing
- `src/bare.js` `handleHello`: emits `child:connected` only when `existingRecord` is null (new peer); emits `child:reconnected` for subsequent reconnects.
- `src/ui/components/ChildrenList.jsx`: subscribes to both events — `child:connected` hides the Add flow and refreshes; `child:reconnected` just refreshes.
- `src/ui/components/__tests__/ParentApp.test.jsx`: two new tests — banner fires on `child:connected`, no banner on `child:reconnected`.

### TODO #33 — Tab highlight no longer lingers in ChildDetail
- `src/ui/components/ChildDetail.jsx` `tabInactive`: added `borderBottom: '2px solid transparent'` so React explicitly clears the active underline on inactive tabs.

### TODO #35 — Tapping "Request Approved" notification opens Requests tab
- `android/.../UsageStatsModule.java` `showDecisionNotification`: replaced `getLaunchIntentForPackage` with a deep link `PendingIntent` for `pear://pearguard/child-requests`.
- `android/app/src/main/AndroidManifest.xml`: added `<intent-filter>` for `pear://pearguard/child-requests`.
- `app/index.tsx`: added `_pendingChildRequestsNav` flag; on `dbReady`, injects `navigate:child:requests` event into WebView.
- `src/ui/components/ChildApp.jsx`: listens for `navigate:child:requests` event and switches active tab to `'requests'`.

### TODO #29 — Suppress app-installed notifications on initial pairing sync
- `src/bare-dispatch.js` `handleIncomingAppsSync`: detects first sync by checking `db.get('policy:' + childPublicKey)` — null means no prior policy (first sync). On first sync, apps are stored in policy but no alerts/events are emitted.

### TODO #25 — Auto-block new app installs until parent approves
- `src/bare-dispatch.js` `handleIncomingAppInstalled`: after storing app as `pending`, looks up `noiseKey` from `peers:{childPublicKey}` and sends `policy:update` to child via `sendToPeer`.
- `src/bare-dispatch.js` `handleIncomingAppsSync`: on incremental sync (non-first), new apps sent via `policy:update` to child.
- `src/bare.js`: `sendToPeer` threaded into both call sites.

### TODOs Added

| # | Description |
|---|-------------|
| #37 | Bug: P2P messages not delivered while parent is backgrounded |
| #38 | Bug: Usage tab not populating any data |
| #39 | Schedules and Time Limits need to work together properly |
| #40 | Tapping "app installed" notification on parent should deep-link to Apps tab |

---

## Notes

- Stale-connection destroy approach was attempted and reverted — `conn.destroy()` triggers Hyperswarm backoff, preventing reconnection. `swarm:reconnect` reverted to plain `swarm.flush()`.
- `bare-universal.bundle` must be `rm`'d before `npm run build:bare` due to `--linked` flag not overwriting stale bundle.
- Tailscale VPN on parent device blocks Hyperswarm UDP/TCP — disable during testing.

---

## Test State

- **214 passing, 10 failing** (all pre-existing)
- Both devices have the latest build installed

## ADB Device Serials

| Device | Serial | Role |
|--------|--------|------|
| Pixel | `53071FDAP00038` | Parent |
| TCL | `4H65K7MFZXSCSWPR` | Child |

---

## Suggested Next TODOs

1. **#40** — Quick fix: `showAppInstalledNotification` should deep-link to Apps tab (infrastructure already exists from #34/#35)
2. **#38** — Usage tab not populating: investigate `UsageTab.jsx`, `UsageStatsModule.getTodayScreenTime`, and usage report events
3. **#22** — Requests showing Pending after approval: verify `handleAppDecision` P2P path works end-to-end
